# Local settings (permissions, MCP config)
settings.local.json
settings.json

# Internal reports (marketing, strategy, worker reports)
reports/

# OS files
.DS_Store

# Whitelist hooks so they're tracked in git (teammates in worktrees need them)
!hooks/
